#include <iostream>
#include <map>
#include <string>
#include <vector>

int main() {
  int cases{0};
  std::cin >> cases;

  while (--cases) {
    int words{};
    std::vector<std::string> guy1, guy2, guy3;
    std::map<std::string, int> wordCount;

    std::cin >> words;
  }
}
